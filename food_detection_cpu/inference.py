from ultralytics import YOLO; import cv2; import os; model = YOLO("runs/detect/train9/weights/best.pt"); os.makedirs("inference_results", exist_ok=True); test_images = ["docs/images/pizza_detected.png", "docs/images/burger_detected.png", "docs/images/sushi_detected.png"]; [cv2.imwrite(os.path.join("inference_results", os.path.basename(img)), model.predict(img, conf=0.25)[0].plot()) for img in test_images]
